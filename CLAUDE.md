# Claude Code

Follow the collection protocol in [`AGENTS.md`](AGENTS.md). It is binding here too.

@AGENTS.md

## Bank selection

- Collection / protocol / template work → `_collection/memory-bank/`
- Work inside an agent folder → that folder's `memory-bank/`
- No shared bank. Do not merge banks across agents.

## Every session

Read `memory-bank/PROTOCOL.md`, `progress.md`, `active-context.md`, and `decisions.md` for the agent you are in. After work, update `progress.md` and `active-context.md`. Lasting decisions go in `decisions.md` as `DEC-NNN`.
